# UI parity checklist (code vs Figma)

Цель: зафиксировать текущий статус приведения реализованного UI к макетам Figma и согласованным токенам из [technical-spec-figma.md](./technical-spec-figma.md).

**Сверка с макетом:** имена фреймов, `node id` и прототип — в technical-spec (аудиты **vibma**: **2026-04-04** + **2026-04-06** — MOD «Добавить в план» / тост / прототип с первой карточки **`SCR / Библиотека рецептов`**). URL файла Figma в репо не хранится; актуальность макета подтверждается аудитом через подключённый плагин.

## Область проверки

- Frontend shell: `frontend/apps/host`
- MFE рецептов: `frontend/apps/mf-recipes`
- MFE планировщика: `frontend/apps/mf-planner`
- MFE покупок: `frontend/apps/mf-shopping`

## Базовые критерии parity

- Шапки **`UiAppHeader`** на экранах host и MFE сверяются с фреймом **`UiAppHeader — все сторис`** (node **`3390:2430`**, секция **C**, страница **`01 — Токены и UI Kit`**).
- Mobile-first: базовые стили для mobile, затем брейкпоинты `768` и `1200`.
- Токены: цвета/радиусы/spacing/типографика только из `@meal/ui-tokens`.
- Touch targets: интерактивные controls не меньше `44px`.
- Состояния: loading/error/empty визуально согласованы и читаемы.
- Навигация и business-flow не ломаются при визуальных изменениях.

## Статус по модулям

### `host`

- Статус: **готово (этап 1)**.
- Выполнено:
  - shell/topbar и навигация переведены на mobile-first;
  - главная (`HomeView`) приведена к карточным паттернам Figma;
  - брейкпоинты `768/1200` и token-based стили применены;
  - **`RegisterView`** выровнен с **`LoginView`**: `UiInput` / `UiButton`, те же отступы и карточка auth; `UiInput` прокидывает `data-testid` и нативные атрибуты на `<input>` для e2e.
- Риски:
  - пиксельная сверка с **`SCR / Вход`** / **`SCR / Регистрация`** по `node id` — по желанию.

### `mf-recipes`

- Статус: **готово (основной сценарий)**.
- Выполнено:
  - `Library`, `RecipeDetail`, `ImportView`, `RecipeForm` приведены к mobile-first;
  - кнопки/поля/карточки/модалки переведены на токены и единый визуальный язык;
  - сетки карточек и форм адаптированы под `768/1200`;
  - модальный флоу **«В план» с карточки библиотеки**: два шага (дата → приём пищи), тост успеха, опциональный переход в планировщик — по макету **`3387:*`** и [design-plan.md](./design-plan.md);
  - **`Library` + `UiRecipeCard`**: слот **`#top`** с `RouterLink` на деталь рецепта, действия в **`#actions`** (паттерн DS: тело карточки → detail, кнопки не внутри ссылки);
  - **`RecipeDetail`**: выбор дня для перехода в планировщик — **`UiModalShell`** (как **`DS / Modal / Shell`**) + месячная сетка Пн–Вс, ячейки **≥48px** и **`var(--touch-target)`**, scrim через токен **`UiModalShell`** (`--color-overlay`).
- Соответствие макету (**2026-04-06**): в коде — **два шага** (заголовки как в Figma), панель-подсказка, **Далее** / **Назад** / **Добавить**; после успешного **PATCH** — тост **«Рецепт добавлен»** с строкой рецепт·дата·слот и кнопками **OK** / **В планировщик** (вторичное действие — опция из [design-plan.md](./design-plan.md), в прототипе Figma только **OK**). Один контейнер `UiModalShell` без стека двух оверлеев — как рекомендовано в technical-spec. **Остаётся:** пиксельная сверка с `node id` и при желании убрать **«В планировщик»**, если дизайн зафиксирует только тост с **OK**.
- Риски:
  - точечная пиксельная сверка остальных экранов с `node id` из technical-spec по желанию.

### `mf-planner`

- Статус: **готово (основной экран)**.
- Выполнено:
  - `PlannerPage` переведён на mobile-first;
  - панель периода, sidebar и недельный layout адаптированы для `768/1200`;
  - token-based кнопки/инпуты/overlay и focus state;
  - **touch-target**: кнопка **`link-remove`** и ячейки календаря **`.cell`** — не ниже **`var(--touch-target)`** / **48px** (см. ТЗ).
  - тач-добавление в план: выбор слота + кнопка **«В слот»** (HTML5 DnD остаётся для мыши).
- Риски:
  - нужен отдельный UX-pass по dense-состояниям (много рецептов в слотах, длинные строки).

### `mf-shopping`

- Статус: **готово (основной экран)**.
- Выполнено:
  - `ShoppingPage` переведён на mobile-first;
  - группы, строки, toolbar и ручной ввод выровнены по токенам;
  - адаптивная компоновка на `768/1200`.
- Риски:
  - полезно добавить визуальный smoke для очень длинных названий/merge-note.

## Минимальный regression checklist

- `host`: переходы `/recipes`, `/planner`, logout/login/register.
- `mf-recipes`: поиск, действия карточки, модалка `В план`, переходы в detail/edit/import.
- `mf-planner`: смена недели, открытие календаря, добавление рецепта в слот, build shopping list.
- `mf-shopping`: toggle purchased, удаление строки, manual add, copy/export.

## Что дальше

1. Точечная сверка **`Library.vue`** с фреймами **`3387:2326`**, **`3387:2342`**, **`3387:2359`** (типографика, отступы); согласовать с дизайном необходимость кнопки **«В планировщик»** на тосте.
2. Добавить e2e-скриншотные проверки на 3 ширинах: mobile/tablet/desktop.
3. При значимых правках Figma прогонять аудит **vibma**, обновлять `technical-spec-figma.md` и при необходимости этот чеклист (в т.ч. блок расхождений код ↔ макет).

## Migration checklist (Wave execution)

- Wave 1:
  - shared primitives вынесены в `frontend/packages/ui-kit`;
  - на каждый публичный компонент есть story + unit test + screenshot check;
  - стили shared-компонентов используют семантические токены.
- Wave 2:
  - host и каждый MFE имеют минимум один мигрированный vertical slice;
  - удалены локальные дубли `button/input/modal` в мигрированных срезах;
  - BFF access в приложениях использует shared adapter policy.
- Wave 3:
  - CI блокирует PR при падении frontend unit/screenshot/e2e checks;
  - остаточные parity-расхождения не блокируют ключевые UX-сценарии.
