global:
  scrape_interval: 15s

scrape_configs:
  - job_name: prometheus
    static_configs:
      - targets: ['localhost:9090']

  # Add targets when services expose /metrics:
  # - job_name: bff-web
  #   static_configs:
  #     - targets: ['host.docker.internal:8080']
